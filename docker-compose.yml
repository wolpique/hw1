version: '3'
services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
  yourapp:
    build: .
    container_name: yourapp
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
